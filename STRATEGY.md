# Two-Leg Lock — Gravia Polymarket BTC Up/Down 策略說明

這個機器人玩的是 Polymarket 每 5 分鐘結算一次的 BTC「Up / Down」市場。核心不是預測漲跌，是在 Up
跟 Down 兩張票同時買得到、加總不到 $1 的瞬間把兩邊都買下來——不管結果是哪邊贏，都穩賺中間那段
價差。找不到這種瞬間時，才退而求其次在窗口最後幾秒賭方向。

## 01 · 主要玩法：兩腿鎖利（two-leg lock）

Polymarket 的 Up 跟 Down 是互補結果——結算時剛好一個值 $1、一個值 $0。正常情況下市場會把兩邊
的賣價撮合到接近 $1（扣掉交易所抽成的空間），但深度淺、報價更新有落差時，偶爾會出現**兩邊賣價
加總低於 $1** 的瞬間。這種瞬間買下兩邊，結算後穩拿 $1，成本卻不到 $1——跟結果無關，是無方向
曝險的價差利潤。

範例：
```
Up ask  $0.42
Down ask $0.50
─────────────────
合計 $0.92 < $1.00（門檻 LOCK_MAX_SUM=0.95）
→ 買兩邊，結算穩拿 $1／股，鎖住 $0.08／股（扣費用前）
```

實作上兩腿是同一次 `POST /orders` batch 送出——公開 API 沒有跨訂單原子性，所以理論上仍可能只有
一腿成交，這正是下面「已知風險」在處理的問題。

## 02 · 備案：晚進場方向性（T-10s）

整個窗口都湊不到能鎖利的價差時，不會空手到底——最後幾秒，方向常常已經定了。窗口剩不到 10 秒、
現價相對開盤價已經明顯偏移，就單邊買進看起來會贏的那一邊，抱到結算，不補鎖利也不提早出場。

| 參數 | 數值 | 意義 |
|---|---|---|
| 進場窗口 | T-10s → T-3s | 剩餘秒數落在這區間才考慮 |
| 最低偏移 | 2% | 現價相對窗口開盤價至少要偏這麼多 |
| 進場價上限 | $0.92 | 超過這個價位不追 |
| 訊號來源 | Chainlink 60s TWAP | 歷史混合變體用這個，早期版本用 Binance 現貨價 |

## 03 · 目前實盤在跑的：歷史混合策略（Historical Hybrid）

兩個玩法合成一條流程，每個窗口從頭跑到尾：

1. **整個窗口優先找鎖利**——每個報價 tick 都檢查 Up+Down 加總，一旦低於 `0.95`、深度足夠、
   報價穩定 `0.15s` 以上，立刻兩腿買進。
2. **沒鎖到 → 最後 10 秒改賭方向**——整個窗口都沒等到鎖利機會，剩 3~10 秒時改用 Chainlink
   TWAP 訊號單邊進場，抱到結算。
3. **兩者都沒有 → 空手**——純方向性賭注（跳過鎖利直接賭）歷史驗證是 0% 勝率，已經確認關閉，
   不會作為退路。

## 04 · 模擬盤與真實下單共用同一套判斷

- **紙上模擬**：吃即時訂單簿快照算成交，零延遲、保證成交——用來快速驗證一個門檻組合值不值得
  測，但不會遇到「深度被搶走」這種真實世界的問題。
- **真實下單**：要跟真人搶同一份流動性，中間有網路延遲、交易所處理時間——同一套邏輯，結果可能
  天差地遠，這個落差本身就是最重要的資料。

兩邊的定價、門檻、股數計算全部走同一份函式；模擬盤多開了好幾組 A/B 門檻、不同資產、不同窗口
長度（15 分鐘／4 小時）持續跑，用來在真的動用真錢調整參數之前，先看哪個方向有機會。

## 05 · 已知的結構性風險（真金白銀踩出來的）

**一腿沒接到**（實測 -$1.66 ～ -$5.04／筆）
Batch 送出兩腿不是原子交易；下單瞬間深度已經跟決策快照不一樣，可能只有一腿成交，變成意外的
單邊曝險。
→ 已加：失敗腿重試一次、緊急平倉用更激進的讓價快速出場、限價內要求數倍於下單量的深度

**延遲已經接近公開 API 的物理下限**（warm keep-alive p50 ≈ 24.6ms，都柏林）
實測都柏林／倫敦／法蘭克福三個地區，都柏林最快；地理上「感覺比較近」不代表真的比較快——多半
跟交易所自己的處理時間、路由路徑有關，不是純距離問題。
→ 結論：再加碼硬體/機房邊際效益很低，除非拿到官方 co-location（需要 KYC/KYB）

**窗口收尾前，交易所直接關閉下單**（實測剩 9.2~9.5 秒即被拒）
晚進場方向性設計上要在窗口快結束時進場，卻可能撞進 Polymarket 自己「結算前禁止下單」的區間——
不是深度不夠，是交易所直接回絕。
→ 仍在觀察：持續蒐集真實拒單的剩餘秒數，尚未找到穩定邊界

**深度會隨時間大幅波動**（同一時刻 5 分鐘窗口深度曾比 15 分鐘、4 小時都深）
原本假設「窗口越長、深度越穩」，實測推翻——深度沒有固定跟窗口長度掛鉤，換更長的窗口不保證
解決一腿沒接到的問題。
→ 結論：不追求「不會淺」的市場，把重點放在深度淺時怎麼降低損失

---
*gravia_live / polymarket_server.py · polymarket_live_strategy.py · 2026-09*
